# Site em inglês (`/en/`) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publicar as 6 páginas do trakapp.com.br em inglês sob `/en/`, ligadas às versões em português por `hreflang` e alternador, com um teste que impede os pares de derivarem.

**Architecture:** Cópia por página (`en/<slug>.html`) traduzida à mão, sem build. Assets compartilhados por caminho absoluto. Um teste em Node lê os 12 arquivos como texto e valida cabeçalho, alternador, links internos, sitemap e `ct=`. Sem JS novo além do teste.

**Tech Stack:** HTML/CSS estáticos, Vercel (`cleanUrls`), Node 20+ (`node js/i18n.test.mjs`, sem dependências), `python3 -m http.server` pra ver local.

**Spec:** `docs/superpowers/specs/2026-09-09-site-en-design.md`

## Global Constraints

- Repo `trak-site`, branch `site-en`. **Commits SEM trailer `Co-Authored-By`** (a Vercel bloqueia o deploy em silêncio; CLAUDE.md).
- Push em `main` = produção. Merge só com OK explícito do Tiago.
- Slugs: `/en` · `/en/set-up` · `/en/accessibility` · `/en/privacy` · `/en/terms` · `/en/press`.
- `x-default` aponta pro português. `og:locale` `en_US` no en, `pt_BR` no pt, `og:locale:alternate` cruzado.
- Badge da home em inglês: `ct=LP-en`. Páginas pt seguem `ct=LP`.
- Copy: liderar com a tela de bloqueio se transformando, nunca com "habit app"; sem "solo Brazilian developer" no discurso de venda (permitido em "quem fez" e nas páginas de responsabilidade); nomes de estilos = coluna `en` de `../trak/marketing/fatos-do-app.md` (Year calendar, Month by month, Semester, Life in weeks, Consistency, Streak number, Pulse, Spiral); não citar número de estilos ("8 modos"); não dizer "3 hábitos grátis" nem "vitalício".
- Fatos (fonte `../trak/marketing/fatos-do-app.md` §Free × Pro): free = **2 hábitos ativos** + Calendário do ano e Mês a mês; trial = **7 dias no mensal, 14 dias no anual**; iOS 26+; iPhone only.
- Nome do atalho é **"Atualizar Wallpaper trak" também em inglês** (o app não localiza esse nome: `trak/Intents/RunWallpaperShortcutIntent.swift`).
- Só o CSS do alternador muda em `style.css`.

---

### Task 0: Corrigir os fatos na home em português antes de traduzir

**Files:**
- Modify: `index.html` (JSON-LD FAQ ~linha 61–63, `.pricing-note` ~linha 198, FAQ ~linha 237–239)

**Interfaces:**
- Produces: `index.html` com os fatos certos, que a Task 4 traduz.

- [ ] **Step 1: Localizar as 4 ocorrências erradas**

Run: `grep -n "3 hábitos\|7 dias de teste" index.html`
Expected: 4 linhas (2 no JSON-LD, 1 no `.pricing-note`, 1 na FAQ visível).

- [ ] **Step 2: Corrigir o texto**

Substituições (manter o resto da frase):
- "até 3 hábitos" → "até 2 hábitos" (JSON-LD e FAQ visível)
- "Grátis: 3 hábitos e 2 estilos, sem prazo." → "Grátis: 2 hábitos e 2 estilos, sem prazo."
- "com 7 dias de teste grátis no plano anual" → "com 14 dias de teste grátis no plano anual (7 no mensal)" (JSON-LD e FAQ visível)
- "com 7 dias de teste no plano anual" (`.pricing-note`) → "com 14 dias de teste no plano anual"

- [ ] **Step 3: Conferir que não sobrou**

Run: `grep -n "3 hábitos\|7 dias de teste" index.html`
Expected: nenhuma linha.

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "home: fatos do free e do trial alinhados ao fatos-do-app (2 hábitos; 14 dias no anual)"
```

---

### Task 1: Teste anti-deriva (falhando)

**Files:**
- Create: `js/i18n.test.mjs`
- Modify: `docs/superpowers/specs/2026-09-09-site-en-design.md` (§9 e §10: o comando é `node js/i18n.test.mjs`, igual ao `canal.test.mjs`, não `node --test`)

**Interfaces:**
- Produces: tabela `PARES` (pt, en, rotaPt, rotaEn) que as Tasks 2–8 devem satisfazer; script sai com código 1 se falhar.

- [ ] **Step 1: Escrever o teste**

```js
// Rode com: node js/i18n.test.mjs
// Garante que cada página em português tem seu par em inglês e que os dois
// apontam um pro outro (hreflang, alternador), sem link "vazando" pro outro
// idioma. Lê os arquivos como texto — sem parser, sem dependência.
import fs from 'node:fs'

const ROOT = new URL('../', import.meta.url)
const APEX = 'https://trakapp.com.br'
const PARES = [
  ['index.html',          'en/index.html',         '/',               '/en'],
  ['configurar.html',     'en/set-up.html',        '/configurar',     '/en/set-up'],
  ['acessibilidade.html', 'en/accessibility.html', '/acessibilidade', '/en/accessibility'],
  ['privacidade.html',    'en/privacy.html',       '/privacidade',    '/en/privacy'],
  ['termos.html',         'en/terms.html',         '/termos',         '/en/terms'],
  ['imprensa.html',       'en/press.html',         '/imprensa',       '/en/press'],
]
const ROTAS_PT = ['/configurar', '/privacidade', '/termos', '/acessibilidade', '/imprensa']

let fails = 0
const check = (nome, ok, detalhe = '') => {
  if (!ok) fails++
  console.log(`${ok ? 'ok  ' : 'FAIL'} ${nome}${ok || !detalhe ? '' : ' — ' + detalhe}`)
}
const read = (p) => fs.existsSync(new URL(p, ROOT)) ? fs.readFileSync(new URL(p, ROOT), 'utf8') : null
const has = (html, s) => html.includes(s)

for (const [pt, en, rotaPt, rotaEn] of PARES) {
  const hp = read(pt), he = read(en)
  check(`${en} existe`, he !== null)
  if (!hp || !he) continue

  // 1. lang
  check(`${pt} lang=pt-BR`, has(hp, '<html lang="pt-BR">'))
  check(`${en} lang=en`, has(he, '<html lang="en">'))

  // 2. canonical no apex
  check(`${pt} canonical`, has(hp, `<link rel="canonical" href="${APEX}${rotaPt}">`))
  check(`${en} canonical`, has(he, `<link rel="canonical" href="${APEX}${rotaEn}">`))

  // 3. hreflang recíproco nos dois arquivos
  for (const [arq, html] of [[pt, hp], [en, he]]) {
    check(`${arq} hreflang pt-BR`, has(html, `<link rel="alternate" hreflang="pt-BR" href="${APEX}${rotaPt}">`))
    check(`${arq} hreflang en`, has(html, `<link rel="alternate" hreflang="en" href="${APEX}${rotaEn}">`))
    check(`${arq} hreflang x-default → pt`, has(html, `<link rel="alternate" hreflang="x-default" href="${APEX}${rotaPt}">`))
  }

  // 4. og:locale
  check(`${pt} og:locale pt_BR`, has(hp, '<meta property="og:locale" content="pt_BR">'))
  check(`${pt} og:locale:alternate en_US`, has(hp, '<meta property="og:locale:alternate" content="en_US">'))
  check(`${en} og:locale en_US`, has(he, '<meta property="og:locale" content="en_US">'))
  check(`${en} og:locale:alternate pt_BR`, has(he, '<meta property="og:locale:alternate" content="pt_BR">'))

  // 5. alternador aponta pro par
  check(`${pt} alternador → ${rotaEn}`, has(hp, `class="nav-lang" href="${rotaEn}" hreflang="en" lang="en"`))
  check(`${en} alternador → ${rotaPt}`, has(he, `class="nav-lang" href="${rotaPt}" hreflang="pt-BR" lang="pt-BR"`))

  // 6. nenhum link interno em en/ aponta pra rota pt do escopo (fora alternador e nota de cortesia)
  const corpo = he
    .replace(/<a class="nav-lang"[^>]*>[^<]*<\/a>/g, '')
    .replace(/<p class="courtesy">[\s\S]*?<\/p>/g, '')
  const vazados = ROTAS_PT.filter((r) => new RegExp(`href="${r}(#[^"]*)?"`).test(corpo))
  check(`${en} sem link pra rota pt`, vazados.length === 0, vazados.join(', '))
  check(`${en} sem link pra home pt`, !/href="\/(#[^"]*)?"/.test(corpo), 'href="/" ou href="/#..." fora do alternador')
}

// 7. sitemap
const sitemap = read('sitemap.xml') || ''
for (const [, , , rotaEn] of PARES) check(`sitemap tem ${rotaEn}`, has(sitemap, `<loc>${APEX}${rotaEn}</loc>`))

// 8. badge e canal.js na home en
const homeEn = read('en/index.html') || ''
check('en/index.html carrega canal.js', has(homeEn, '<script defer src="/js/canal.js"></script>'))
const badgesEn = homeEn.match(/apps\.apple\.com[^"]*/g) || []
check('en/index.html tem badges', badgesEn.length >= 1)
check('badges en com ct=LP-en', badgesEn.every((b) => b.includes('&ct=LP-en&')), badgesEn.join(' | '))

// 9. páginas pt do escopo seguem ct=LP
for (const [pt] of PARES) {
  const badges = (read(pt) || '').match(/apps\.apple\.com[^"]*/g) || []
  check(`${pt} badges ct=LP`, badges.every((b) => b.includes('&ct=LP&')), badges.join(' | '))
}

console.log(fails ? `\n${fails} falha(s)` : '\ntudo ok')
process.exitCode = fails ? 1 : 0
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node js/i18n.test.mjs | tail -3`
Expected: "N falha(s)" com N > 0 (nenhum `en/*.html` existe ainda).

- [ ] **Step 3: Corrigir o comando na spec**

Em `docs/superpowers/specs/2026-09-09-site-en-design.md`, trocar `node --test js/` por `node js/canal.test.mjs && node js/i18n.test.mjs` (§3, §9, §10).

- [ ] **Step 4: Commit**

```bash
git add js/i18n.test.mjs docs/superpowers/specs/2026-09-09-site-en-design.md
git commit -m "i18n: teste anti-deriva dos pares pt/en (falhando até as páginas existirem)"
```

---

### Task 2: Alternador, CSS e hreflang nas 6 páginas em português

**Files:**
- Modify: `style.css` (media query da linha ~58)
- Modify: `index.html`, `configurar.html`, `acessibilidade.html`, `privacidade.html`, `termos.html`, `imprensa.html` (`<head>` e `<nav class="nav-links">`)

**Interfaces:**
- Produces: classe `.nav-lang` (CSS) e o padrão de `<head>` que as páginas en repetem.

- [ ] **Step 1: CSS do alternador**

Em `style.css`, a regra `@media(max-width:640px){ .nav-links a:not(.nav-cta){display:none;} }` vira:

```css
@media(max-width:640px){ .nav-links a:not(.nav-cta):not(.nav-lang){display:none;} }
.nav-links a.nav-lang{font-size:.8rem; letter-spacing:.06em; opacity:.75;}
```

- [ ] **Step 2: Head de cada página pt**

Logo após a linha do `<link rel="canonical" …>`, inserir (trocando `<rotaPt>`/`<rotaEn>` pelo par da tabela da Task 1):

```html
  <link rel="alternate" hreflang="pt-BR" href="https://trakapp.com.br/<rotaPt>">
  <link rel="alternate" hreflang="en" href="https://trakapp.com.br/<rotaEn>">
  <link rel="alternate" hreflang="x-default" href="https://trakapp.com.br/<rotaPt>">
```

Pares: `/` ↔ `/en` · `/configurar` ↔ `/en/set-up` · `/acessibilidade` ↔ `/en/accessibility` · `/privacidade` ↔ `/en/privacy` · `/termos` ↔ `/en/terms` · `/imprensa` ↔ `/en/press`.

Nas páginas que já têm `<meta property="og:locale" content="pt_BR">` (index, configurar), acrescentar logo abaixo `<meta property="og:locale:alternate" content="en_US">`. Nas que não têm bloco OG (acessibilidade, privacidade, termos, imprensa), acrescentar as duas linhas após o canonical:

```html
  <meta property="og:locale" content="pt_BR">
  <meta property="og:locale:alternate" content="en_US">
```

- [ ] **Step 3: Alternador na nav**

Em cada página, como ÚLTIMO filho de `<nav class="nav-links">` (depois do `.nav-cta`):

```html
      <a class="nav-lang" href="<rotaEn>" hreflang="en" lang="en" aria-label="Read in English">EN</a>
```

- [ ] **Step 4: Rodar os testes**

Run: `node js/canal.test.mjs && node js/i18n.test.mjs | grep -v '^ok'`
Expected: canal ok; no i18n, as linhas `*.html lang=pt-BR`, `canonical`, `hreflang`, `og:locale`, `alternador →` das páginas pt passam; só os `en/*.html existe` e os derivados falham.

- [ ] **Step 5: Ver local**

Run: `python3 -m http.server 8765` e abrir `http://localhost:8765/index.html` numa janela < 640px: o "EN" aparece ao lado de "Baixar".

- [ ] **Step 6: Commit**

```bash
git add style.css index.html configurar.html acessibilidade.html privacidade.html termos.html imprensa.html
git commit -m "i18n: hreflang, og:locale e alternador EN nas 6 páginas em português"
```

---

### Task 3: Modelo do `<head>`, nav e rodapé em inglês (usado pelas Tasks 4–8)

Esta task não cria arquivo: define os blocos exatos. Cada página en copia daqui.

**Head (trocar `<rotaPt>`, `<rotaEn>`, título e description):**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>…</title>
  <meta name="description" content="…">
  <link rel="canonical" href="https://trakapp.com.br/<rotaEn>">
  <link rel="alternate" hreflang="pt-BR" href="https://trakapp.com.br/<rotaPt>">
  <link rel="alternate" hreflang="en" href="https://trakapp.com.br/<rotaEn>">
  <link rel="alternate" hreflang="x-default" href="https://trakapp.com.br/<rotaPt>">
  <meta name="theme-color" content="#f6f4ef">
  <meta property="og:locale" content="en_US">
  <meta property="og:locale:alternate" content="pt_BR">
  <link rel="icon" href="/favicon.svg" type="image/svg+xml">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png">
  <link rel="apple-touch-icon" href="/img/apple-touch-icon.png">
  <link rel="stylesheet" href="/style.css">
  <script defer src="/_vercel/insights/script.js"></script>
</head>
```

(A home en mantém também o bloco OG/Twitter completo, o preload da fonte, os dois JSON-LD e o `<script defer src="/js/canal.js">`, como a home pt.)

**Nav (páginas internas):**

```html
  <header class="nav"><div class="container">
    <a href="/en" class="brand"><img class="mark" src="/img/logo-mark.svg" width="28" height="28" alt=""> trak</a>
    <nav class="nav-links">
      <a href="/en#how">How it works</a>
      <a href="/en/set-up">Set up</a>
      <a href="/en#download" class="nav-cta">Download</a>
      <a class="nav-lang" href="<rotaPt>" hreflang="pt-BR" lang="pt-BR" aria-label="Ler em português">PT</a>
    </nav>
  </div></header>
```

Na home en, os links da nav são `#how`, `#styles`, `/en/set-up`, `#download` (âncoras locais), mais o alternador com `href="/"`.

**Rodapé (todas as páginas en):**

```html
  <footer class="site"><div class="container inner">
    <div>
      <div class="links"><a href="/en#how">How it works</a><a href="/en/set-up">Set up</a><a href="/en/privacy">Privacy</a><a href="/en/terms">Terms</a><a href="/en/press">Press</a><a href="/en/accessibility">Accessibility</a></div>
      <div style="margin-top:14px;">Contact: <a href="mailto:contato@trakapp.com.br">contato@trakapp.com.br</a></div>
    </div>
    <div class="copy">© 2026 trak · made by Tiago Villas</div>
  </div></footer>
```

Na home en o primeiro link do rodapé é `#how` e há também `#styles` (âncoras locais).

**Breadcrumb (páginas internas):** `<p class="breadcrumb"><a href="/en">Home</a> › <Título></p>`.

**Nota de cortesia (só privacy e terms), logo abaixo do `<p class="updated">`:**

```html
    <p class="courtesy">This is a courtesy translation. If there is any conflict, the Portuguese version at <a href="/privacidade" hreflang="pt-BR" lang="pt-BR">trakapp.com.br/privacidade</a> governs.</p>
```

(em `terms.html`, `/termos`). O teste ignora links dentro de `<p class="courtesy">`.

---

### Task 4: `en/index.html` (home)

**Files:**
- Create: `en/index.html` (a partir de `index.html`)

- [ ] **Step 1: Copiar**

Run: `mkdir -p en && cp index.html en/index.html`

- [ ] **Step 2: Head**

Aplicar o modelo da Task 3 com `<rotaPt>` = `/`, `<rotaEn>` = `/en`. Manter o bloco Pinterest `p:domain_verify`. Traduzir:
- `<title>`: `trak — the habit tracker that becomes your iPhone wallpaper`
- description: `trak turns your habits into your wallpaper: a calendar that fills in on your iPhone Lock Screen, one cell at a time.`
- OG/Twitter: `og:url` = `https://trakapp.com.br/en`, títulos/descrições em inglês (mesma ideia), `og:image` igual.
- JSON-LD `MobileApplication`: `"url": "https://trakapp.com.br/en"`, `"inLanguage": "en"`, description em inglês.
- JSON-LD `FAQPage`: as 5 perguntas traduzidas, com os fatos das Global Constraints (2 habits, 2 styles; 7-day trial monthly, 14-day annual; iPhone only, iOS 26+).

- [ ] **Step 3: Nav, âncoras e rodapé**

Renomear os `id` das seções: `como` → `how`, `estilos` → `styles`, `baixar` → `download`; atualizar todos os `href="#…"`. Nav e rodapé conforme Task 3 (home: âncoras locais + `/en/set-up`; alternador `href="/"`).

- [ ] **Step 4: Corpo**

Traduzir todo texto visível (hero, como funciona, vídeo, estilos, trio, quem fez, FAQ, CTA), com as regras de copy das Global Constraints. O bloco `.ls-cal` do hero fica igual. Os dois badges: `ct=LP` → `ct=LP-en`; `aria-label="Download on the App Store"`. Nomes de estilos pela coluna `en` do fatos-do-app.

- [ ] **Step 5: Testes**

Run: `node js/canal.test.mjs && node js/i18n.test.mjs | grep -v '^ok'`
Expected: todas as linhas de `index.html`/`en/index.html` e as de badge/canal.js ok; falhas só dos outros 5 `en/*.html`.

- [ ] **Step 6: Ver local**

Run: `python3 -m http.server 8765` → `http://localhost:8765/en/index.html`. Conferir hero animado, âncoras da nav, alternador "PT" levando a `/` (local abre 404 na URL limpa: esperado).

- [ ] **Step 7: Commit**

```bash
git add en/index.html
git commit -m "en: home"
```

---

### Task 5: `en/set-up.html` (configurar)

**Files:**
- Create: `en/set-up.html` (a partir de `configurar.html`)

- [ ] **Step 1: Copiar e aplicar head/nav/rodapé**

Run: `cp configurar.html en/set-up.html`. Head da Task 3 com `/configurar` ↔ `/en/set-up`. `<title>`: `How to put your habits on your iPhone Lock Screen — trak`. Description: `Step by step to get the trak habit calendar on your iPhone Lock Screen wallpaper: install the shortcut, create the daily automation in the Shortcuts app, and test it.` JSON-LD `HowTo` traduzido (`inLanguage: "en"`).

- [ ] **Step 2: Nomes reais do iOS em inglês**

Antes de traduzir os passos, abrir o app Shortcuts num simulador em en-US e anotar os rótulos exatos: aba `Automation`, botão `+`, tipo `Time of Day`, `Daily`, ação `Run Shortcut`, opção `Run Immediately`. Como abrir: `xcrun simctl boot "iPhone 17"; xcrun simctl launch booted com.apple.shortcuts` (o simulador do Xcode 26.5 vem em inglês). Se algum rótulo diferir do listado, usar o do simulador.

- [ ] **Step 3: Corpo**

Traduzir; manter `<span class="kbd">` com os rótulos do Step 2. O nome do atalho continua **"Atualizar Wallpaper trak"** (não traduzir, é o nome que o usuário vê). Horário `00:05` igual.

- [ ] **Step 4: Testes e visual**

Run: `node js/i18n.test.mjs | grep -v '^ok'` — linhas de `configurar.html`/`en/set-up.html` ok. Abrir `http://localhost:8765/en/set-up.html`.

- [ ] **Step 5: Commit**

```bash
git add en/set-up.html
git commit -m "en: set-up"
```

---

### Task 6: `en/accessibility.html`

**Files:**
- Create: `en/accessibility.html` (a partir de `acessibilidade.html`)

- [ ] **Step 1: Copiar, head, nav, rodapé** — `/acessibilidade` ↔ `/en/accessibility`. `<title>`: `Accessibility — trak`. Description: `What trak supports in accessibility today, what it does not yet, and how that was tested. Accessibility statement for the trak app.`
- [ ] **Step 2: Corpo** — traduzir; nomes dos recursos do iOS em inglês (VoiceOver, Voice Control, Dynamic Type, Reduce Motion, Grayscale); `<p class="updated">Last updated: September 9, 2026</p>`. Aqui o contexto "built by one person" é permitido, como no pt.
- [ ] **Step 3: Testes e visual** — `node js/i18n.test.mjs | grep -v '^ok'`; abrir local.
- [ ] **Step 4: Commit**

```bash
git add en/accessibility.html
git commit -m "en: accessibility"
```

---

### Task 7: `en/privacy.html` e `en/terms.html`

**Files:**
- Create: `en/privacy.html` (a partir de `privacidade.html`), `en/terms.html` (a partir de `termos.html`)

- [ ] **Step 1: Copiar, head, nav, rodapé** — `/privacidade` ↔ `/en/privacy` (`<title>`: `Privacy Policy — trak`); `/termos` ↔ `/en/terms` (`<title>`: `Terms of Use — trak`).
- [ ] **Step 2: Nota de cortesia** — o `<p class="courtesy">` da Task 3 logo abaixo de `<p class="updated">` em cada uma (privacy → `/privacidade`; terms → `/termos`).
- [ ] **Step 3: Corpo** — traduzir fielmente; manter "LGPD (Brazilian General Data Protection Law)" na primeira menção; datas em inglês (`Last updated: September 4, 2026` / `July 14, 2026`); nomes de produto iguais (trak Pro, Supabase, PostHog, Apple).
- [ ] **Step 4: Testes e visual** — `node js/i18n.test.mjs | grep -v '^ok'`; conferir que o link da nota não conta como vazamento (o teste remove `<p class="courtesy">`).
- [ ] **Step 5: Commit**

```bash
git add en/privacy.html en/terms.html
git commit -m "en: privacy e terms (courtesy translation, pt governs)"
```

---

### Task 8: `en/press.html`

**Files:**
- Create: `en/press.html` (a partir de `imprensa.html`)

- [ ] **Step 1: Copiar, head, nav, rodapé** — `/imprensa` ↔ `/en/press`. `<title>`: `Press — trak`. Description: `trak press kit: facts, screenshots, video and logo of the habit tracker that turns your consistency into your iPhone wallpaper.`
- [ ] **Step 2: Corpo** — traduzir; os assets em `press/` são os mesmos (usar os arquivos EN onde houver par pt/EN); `<p class="updated">Updated September 6, 2026 (version 1.1.0)</p>`; contexto do dev solo permitido aqui.
- [ ] **Step 3: Testes e visual** — `node js/i18n.test.mjs | tail -1` → `tudo ok`. Abrir local.
- [ ] **Step 4: Commit**

```bash
git add en/press.html
git commit -m "en: press"
```

---

### Task 9: Sitemap, llms.txt, README

**Files:**
- Modify: `sitemap.xml`, `llms.txt`, `README.md`

- [ ] **Step 1: Sitemap** — acrescentar 6 `<url><loc>https://trakapp.com.br/en/…</loc></url>` (`/en`, `/en/set-up`, `/en/accessibility`, `/en/privacy`, `/en/terms`, `/en/press`), no mesmo formato das entradas atuais.
- [ ] **Step 2: llms.txt** — seção `## English` com `English version: https://trakapp.com.br/en` e as 6 rotas, uma por linha.
- [ ] **Step 3: README** — em "Estrutura", linha `en/  versões em inglês das 6 páginas (home, set-up, accessibility, privacy, terms, press); pares ligados por hreflang; teste: node js/i18n.test.mjs`; em "Página nova: a lista completa" do CLAUDE.md, item 8: `Se a página tiver par em inglês, criar en/<slug>.html e adicionar o par em js/i18n.test.mjs`.
- [ ] **Step 4: Testes** — `node js/canal.test.mjs && node js/i18n.test.mjs | tail -1` → `tudo ok`.
- [ ] **Step 5: Commit**

```bash
git add sitemap.xml llms.txt README.md CLAUDE.md
git commit -m "i18n: sitemap, llms.txt e docs com as rotas /en"
```

---

### Task 10: Gate visual, PR e verificação em produção

- [ ] **Step 1: Frame pro Tiago** — com o servidor local rodando, capturar a home en em 390px e em desktop: `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --screenshot=/tmp/en-home-mobile.png --window-size=390,2200 http://localhost:8765/en/index.html` (e `--window-size=1280,2400` pro desktop). Mandar os dois pro Tiago; esperar aprovação explícita.
- [ ] **Step 2: PR** — `git push -u origin site-en` e `gh pr create --title "site: versão em inglês em /en (6 páginas, hreflang, alternador)" --body "<narrativa: 4 decisões da spec, Task 0 dos fatos, teste i18n, como verificado>"`. Sem trailer de co-autor. Merge só com OK do Tiago.
- [ ] **Step 3: Depois do merge (Tiago autoriza)** — esperar ~60 s e rodar:

```bash
for r in / /en /en/ /en/set-up /en/accessibility /en/privacy /en/terms /en/press; do
  printf "%s → " "$r"; curl -s -o /dev/null -w "%{http_code}\n" "https://trakapp.com.br$r"
done
```

Expected: 200 em todas (a `/en/` com barra pode responder 308 → `/en`; aceitável). Se alguma der 404 por mais de 2 min, é o bloqueio de autor da Vercel: conferir `git log --format=%b -1` por trailer e recommitar sem ele.

- [ ] **Step 4: Fechar** — no repo trak, mover o card do board pra Feito com data; anotar no card a data do deploy e o resultado do curl.
